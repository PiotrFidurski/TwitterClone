mutation acceptInvitation($conversationId: String!) {
  acceptInvitation(conversationId: $conversationId) {
    id
    conversationId
    type
    acceptedInvitation
    members {
      username
      id
      avatar
    }
  }
}
