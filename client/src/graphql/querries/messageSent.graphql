subscription messageSent($conversationId: String!) {
  messageSent(conversationId: $conversationId) {
    id
    conversationId
    messagedata {
      text
      receiverId
      id
      conversationId
      senderId
    }
  }
}
