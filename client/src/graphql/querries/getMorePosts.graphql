mutation loadMorePosts($postId: ID!) {
  loadMorePosts(postId: $postId) {
    id
    body
    conversationId
    inReplyToId
    ...postLikesFields
    ...postConversationFields
    owner {
      id
      username
      name
      email
      ...userAvatarFields
    }
  }
}
