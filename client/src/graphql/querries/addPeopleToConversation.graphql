mutation addPeopleToConversation($conversationId: ID, $userIds: [ID!]!) {
  addPeopleToConversation(conversationId: $conversationId, userIds: $userIds) {
    id
    conversationId
    type
    members {
      username
      id
      avatar
    }
  }
}
