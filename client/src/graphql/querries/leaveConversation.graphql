mutation leaveConversation($conversationId: String!) {
  leaveConversation(conversationId: $conversationId) {
    id
    conversationId
    lastReadMessageId
    mostRecentEntryId
    oldestEntryId
    type
    participants {
      userId
      lastReadMessageId
    }
  }
}
