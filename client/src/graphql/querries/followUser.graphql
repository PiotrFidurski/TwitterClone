mutation followUser($id: ID!) {
  followUser(id: $id) {
    node {
      ... on User {
        id
        # username
        # email
        # name
        ...userFollowerFields
        ...userAvatarFields
      }
    }
  }
}
